---
docker:
  host: "https://127.0.0.1:2376"
  timeout: 10

stack_configs:
  -
    name: debian_jessie
    container_configs:
      -
        name: debian
        version: 1.0
        dockerfile: ./Dockerfile
