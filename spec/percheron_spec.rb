require 'spec_helper'

describe Percheron do
  it 'has a version number' do
    expect(Percheron::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
